<style>
  .float-nav{
    background: #f9f9f9;
    position: sticky;
    top: 0;
    z-index: 6;
    transition: all .4s ease;
  }
  .hide-header .float-nav{
    margin-top: -80px;
  }
  .float-nav .float-nav-cont{
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .float-nav .float-nav-left{
    display: flex;
  }
  .float-nav .float-nav-item{
    font-size: 20px;
    color: #000;
    padding: 25px 0;
    line-height: 1.2;
    position: relative;
    margin-right: 45px;
    cursor: pointer;
  }
  .float-nav .float-nav-item::before{
    position: absolute;
    content: '';
    display: block;
    height: 2px;
    width: 0;
    background: #000;
    left: 0;
    bottom: 0;
    transition: all .4s ease;
  }
  .float-nav .float-nav-item.active::before,.float-nav .float-nav-item:hover::before{
    width: 100%;
  }
  .float-nav .float-nav-right{
    display: flex;
    align-items: center;
    position: relative;
  }
  .float-nav .down-color-select{
    position: absolute;
    width: 160px;
    left: 0;
    top: 100%;
    background: #fff;
    font-size: 16px;
    color: #000;
    line-height: 1.2;
    z-index: 3;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.03);
    display: none;
  }

  .float-nav .down-color-item{
    padding: 10px 8px;
    display: flex;
    cursor: pointer;
  }

  .float-nav .down-color-item span{
    display: block;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    margin-right: 5px;
  }

  .float-nav .slect-color{
    background: #fff;
    padding: 0 10px;
    cursor: pointer;
    font-size: 14px;
    color: #000;
    width: 160px;
    height: 45px;
    position: relative;
    padding-right: 40px;
    display: flex;
    align-items: center;
  }
  .float-nav .slect-color span{
    width: 20px;
    height: 20px;
    border-radius: 50%;
    display: block;
    margin-right: 5px;
  }
  .float-nav .slect-color::before{
    position: absolute;
    content: '';
    display: block;
    width: 10px;
    height: 10px;
    border-top: 2px solid #000;
    border-right: 2px solid #000;
    transform: translateY(-50%) rotate(135deg);
    top: 50%;
    right: 10px;
  }
  .float-nav .float-add-cart{
    width: 160px;
    height: 45px;
    background: #000;
    color: #fff;
    font-size: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-left: 20px;
    cursor: pointer;
  }

  /* .float-nav .float-add-cart:hover{
    color: #000;
  } */
  .float-nav .float-add-cart:hover span,
  .float-nav .float-add-cart:hover .btn-txt::before {
    color: #000;
  }
  .float-nav .float-nav-mb{
    display: none;
  }

  @media (max-width:1200px) {
    .float-nav{
        /* top: 79px; */
    }
  }

  @media (max-width:740px) {
    .float-nav{
        /* top: 59px; */
    }
    .float-nav .slect-color,.float-nav .float-add-cart{
        width: 120px;
        height: 40px;
        font-size: 12px;

    }
    .float-nav .slect-color{
        padding-right: 30px;
        display: none;
    }
    .float-nav .float-add-cart{
        margin-left: 10px;
    }
  }

  @media (max-width:991px) {
    .float-nav .float-nav-cont{
        padding: 10px 0;
    }
    .float-nav .float-nav-left{
        display: none;
        position: absolute;
        width: 100%;
        background: #fff;
        left: 0;
        top: 100%;
    }
    .float-nav .float-nav-mb{
        font-size: 12px;
        color: #000;
        position: relative;
        cursor: pointer;
        height: 40px;
        display: flex;
        align-items: center;
        background: #fff;
        padding-left:15px;
        padding-right: 80px;
        width: calc(50% - 8px);
        margin-right: 16px;
    }
    .float-nav .float-nav-right{
        width: calc(50% - 8px);
    }

    .float-nav .float-add-cart{
        margin-left: 0;
        width: 100%;
    }

    .float-nav .float-nav-mb::after{
        position: absolute;
        content: '';
        display: block;
        width: 10px;
        height: 10px;
        border-top: 2px solid #000;
        border-right: 2px solid #000;
        transform: translateY(-50%) rotate(135deg);
        top: 50%;
        right: 10px;
        margin-top: -2px;
    }
    .float-nav .float-nav-item{
        padding: 15px var(--container-gutter);
        font-size: 14px;
        border-bottom: 1px solid #eee;
        margin-right: 0;
    }
    .float-nav .float-nav-item::before{
        height: 1px;
    }
  }
</style>
<section class="section-box">
  <div class="float-nav">
    <div class="container">
      <div class="float-nav-cont to-top">
        <div class="float-nav-mb ">{{ section.settings.text }}</div>
        <div class="float-nav-left">
          {%- if block.settings.text != blank -%}
            <div class="float-nav-item active to-top" style="transition-delay: 0.1s" data-route="{{ section.settings.text14 }}">{{ section.settings.text }}</div>
          {%- endif -%}
          {%- if block.settings.text1 != blank -%}
            <div class="float-nav-item to-top" style="transition-delay: 0.2s" data-route="{{ section.settings.text15 }}">{{ section.settings.text1 }}</div>
          {%- endif -%}
          {%- if block.settings.text2 != blank -%}
            <div class="float-nav-item to-top" style="transition-delay: 0.3s" data-route="{{ section.settings.text16 }}">{{ section.settings.text2 }}</div>
          {%- endif -%}
          {%- if block.settings.text3 != blank -%}
            <div class="float-nav-item to-top" style="transition-delay: 0.4s" data-route="{{ section.settings.text17 }}">{{ section.settings.text3 }}</div>
          {%- endif -%}
        </div>
        <div class="float-nav-right to-top" style="transition-delay: 0.5s">
          <div class="slect-color" data-id="{{ section.settings.text6 }}">
            <span style="background: {{ section.settings.text5 }}"></span>
            {{ section.settings.text4 }}
          </div>
          <div class="down-color-select">
            {%- if block.settings.text4 != blank -%}
              <div class="down-color-item" data-id="{{ section.settings.text6 }}">
                <span style="background: {{ section.settings.text5 }}"></span>
                {{ section.settings.text4 }}
              </div>
            {%- endif -%}
            {%- if block.settings.text7 != blank -%}
              <div class="down-color-item" data-id="{{ section.settings.text9 }}">
                <span style="background: {{ section.settings.text8 }}"></span>
                {{ section.settings.text7 }}
              </div>
            {%- endif -%}
            {%- if block.settings.text10 != blank -%}
              <div class="down-color-item" data-id="{{ section.settings.text12 }}">
                <span style="background: {{ section.settings.text11 }}"></span>
                {{ section.settings.text10 }}
              </div>
            {%- endif -%}
          </div>
          {%- if block.settings.text13 != blank -%}
            <a class="float-add-cart btn-diy to-top" style="transition-delay: 0.6s">
              <div class="btn-txt" text="{{ section.settings.text13 }}">
                  <span>{{ section.settings.text13 }}</span>
              </div>
            </a>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</section>
<script>
  (function() {
    window.addEventListener('load',function(){
      $('.float-nav .float-nav-item').click(function(){
        let name = $(this).attr('data-route')
        if($(this).attr('disabled')) return
        $(this).addClass('active').siblings().removeClass('active')
        $("html,body").animate({scrollTop: $('.'+name).offset().top - 150}, 'slow');
        $(this).closest('.float-nav-left').siblings('.float-nav-mb').html($(this).text())
        if($(window).innerWidth()<991){
          $(this).closest('.float-nav-left').slideUp()
        }
      })
      $('.float-nav .float-nav-mb').click(function(){
        $(this).toggleClass('active').siblings('.float-nav-left').slideToggle()
      })
      $('.float-nav .slect-color').click(function(){
        $(this).toggleClass('active')
        $(this).siblings('.down-color-select').slideToggle()
      })
      $('.float-nav .down-color-item').click(function(){
        $('.down-color-select').slideUp()
        $('.float-nav .slect-color').html($(this).html()).attr('data-id',$(this).attr('data-id'))
      });
    })
    // let lastScrollTop = 0;
    // window.addEventListener("scroll", function() {
    //   let {top} = document.querySelector('.float-nav').getBoundingClientRect()
    //   if(top<=70){
    //     document.querySelector('body').classList.add('hide-header')
    //   }else{
    //     document.querySelector('body').classList.remove('hide-header')
    //   }
    // }, false);

    document.addEventListener('DOMContentLoaded', function() {

		    // 监听页面滚动事件
		    window.addEventListener('scroll', function() {
		        // 获取当前可视区域的顶部位置
		        const scrollPosition = window.scrollY || window.pageYOffset;

            let {top} = document.querySelector('.float-nav').getBoundingClientRect()
              if(top<=70){
                document.querySelector('body').classList.add('hide-header')
              }else{
                document.querySelector('body').classList.remove('hide-header')
              }
              document.querySelectorAll('.float-nav .float-nav-item').forEach(item => {
		                    item.classList.remove('active');
		                });
		        // 遍历每个 section
		        document.querySelectorAll('.route-name').forEach(section => {
		            const sectionTop = section.offsetTop;
		            const sectionHeight = section.offsetHeight;
		
		            // 检查是否在可视区域内
		            if (scrollPosition >= sectionTop -100 && scrollPosition < (sectionTop + sectionHeight)) {
		                // 获取当前 section 的 ID
		                const currentId = section.getAttribute('data-route');
		                // 移除之前高亮的导航
		                document.querySelectorAll('.float-nav .float-nav-item').forEach(item => {
		                    item.classList.remove('active');
		                });
		
		                // 高亮对应导航
		                document.querySelector(`.float-nav .float-nav-item.${currentId}`).classList.add('active');
		            }
		        });
		    });
		});
  })();
</script>
{% schema %}
  {
    "name": "float-nav-diy",
    "class": "float-nav-diy",
    "blocks": [],
    "settings": [
      {
        "type": "text",
        "id": "text",
        "label": "Nav"
      },
      {
        "type": "text",
        "id": "text14",
        "label": "Name"
      },
      {
        "type": "text",
        "id": "text1",
        "label": "Nav"
      },
      {
        "type": "text",
        "id": "text15",
        "label": "Name"
      },
      {
        "type": "text",
        "id": "text2",
        "label": "Nav"
      },
      {
        "type": "text",
        "id": "text16",
        "label": "Name"
      },
      {
        "type": "text",
        "id": "text3",
        "label": "Nav"
      },
      {
        "type": "text",
        "id": "text17",
        "label": "Name"
      },
      {
        "type": "text",
        "id": "text4",
        "label": "Color Text"
      },
      {
        "type": "text",
        "id": "text5",
        "label": "Color Code"
      },
      {
        "type": "text",
        "id": "text6",
        "label": "Product ID"
      },
      {
        "type": "text",
        "id": "text7",
        "label": "Color Text"
      },
      {
        "type": "text",
        "id": "text8",
        "label": "Color Code"
      },
      {
        "type": "text",
        "id": "text9",
        "label": "Product ID"
      },
      {
        "type": "text",
        "id": "text10",
        "label": "Color Text"
      },
      {
        "type": "text",
        "id": "text11",
        "label": "Color Code"
      },
      {
        "type": "text",
        "id": "text12",
        "label": "Product ID"
      },
      {
        "type": "text",
        "id": "text13",
        "label": "Button Text"
      }
    ],
    "presets": [
      {
        "name": "float-nav-diy",
        "blocks": []
      }
    ]
  }
{% endschema %}